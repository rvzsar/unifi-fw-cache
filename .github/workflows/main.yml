name: Cache UniFi U7PG2 Firmware
on:
  schedule:
    - cron: '0 0 * * 0'  # Запуск каждое воскресенье в полночь по UTC
  workflow_dispatch:      # Позволяет запускать задачу вручную из интерфейса GitHub
jobs:
  cache-firmware:
    runs-on: ubuntu-latest
    env:
      DEVICE_CODE: 'U7PG2'  # Целевой код устройства
      FIRMWARE_CATALOG_URL: 'https://fw-update.ubnt.com/api/firmware-latest?filter=eq~~product~~unifi-firmware&filter=eq~~channel~~release&limit=5000'
    steps:
      - name: 📥 Клонировать репозиторий
        uses: actions/checkout@v4
      - name: ⚙️ Установить Python и зависимости
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl
      - name: 🔍 Получить каталог прошивок и найти U7PG2
        run: |
          # Скачиваем актуальный каталог через API
          curl -s -o firmware_api.json "$FIRMWARE_CATALOG_URL"
          # Ищем информацию о прошивке для U7PG2
          cat firmware_api.json | jq -e --arg dev "$DEVICE_CODE" '._embedded.firmware[] | select(.platform == $dev)' > firmware_info.json || echo "Прошивка для $DEVICE_CODE не найдена в каталоге" && exit 0
      - name: ⬇️ Скачать прошивку
        run: |
          # Извлекаем URL и версию
          FW_URL=$(jq -r '._links.data.href' firmware_info.json)
          FW_VERSION=$(jq -r '.version' firmware_info.json)
          FW_FILENAME="U7PG2_${FW_VERSION}.bin"
          echo "Скачивание: $FW_VERSION"
          curl -s -L -o "$FW_FILENAME" "$FW_URL"
          echo "Скачано: $FW_FILENAME"
      - name: 📝 Создать Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "${{ env.DEVICE_CODE }}-$(date +%Y-%m-%d)"
          name: "UniFi U7PG2 Firmware $(date +%Y-%m-%d)"
          body: |
            Автоматически загруженная прошивка для устройства U7PG2.
            Ссылка на скачивание в релизе.
          files: "*.bin"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 🧹 Удалить временные файлы
        run: rm -f firmware_api.json firmware_info.json *.bin
